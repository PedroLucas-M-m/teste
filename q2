import ipaddress

entrada = input('Digite seu endereco ip: ')

if '.' in entrada:
    ip_inteiro = int(ipaddress.ip_address(entrada))
else:
    ip_str = str(ipaddress.ip_address(int(entrada))

print(ip_inteiro)
ip_str = str(ipaddress.ip_address(ip_inteiro)
print(ip_str)
